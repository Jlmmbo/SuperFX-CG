- properly handle/set P.V
- peripherals:
    - ppu:
        - add a buffer that will contain last scanlines' tile indexes to check first. remove the ones that are no longer on the scanline
    - other memory mapped peripherals
    - cartrige hardware:
        - coprocessors
        - battery backed ram
        - etc.
- GUI
    - Backrounds
    - fonts
    - use list_menu_ui for keybinds instead of cycling though them
- bgn_px = wrong thing !! deal w/ bitplanes
- different bg modes
- different bpp modes(currently only 4bpp)
- a better way to deal with 8/16 bit values when in native/emulation mode
- use macros to simplify instruction functions ?????
- use timers to call ppu_dot and cycle_cpu (make sure to uninstall them before fs stuff and on exit)
- possible (dangerous) optimization: for rom file, use ptr instead of handle to bypass 1mb read_file_os hang
- use arithmetic type for addresses
- fetch single pages (or last 5 for example) into ram if possible (not banks)
- fix rom list display